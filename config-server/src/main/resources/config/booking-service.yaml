spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/db_booking
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
