spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/db_resource
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  flyway:
    enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
